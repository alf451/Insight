"""Insight MQTT Discovery Probe.

Standalone, read-only tool used at the client site to discover the real
MQTT protocol spoken by a Sesotec metal detector (topics, payload shape,
candidate sensitivity fields) before any mapping is implemented in Insight.

This package intentionally contains NO Sesotec-specific assumptions.
"""

__version__ = "0.1.0"
