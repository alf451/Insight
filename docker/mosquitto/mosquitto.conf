# Local test broker only — anonymous access, no TLS.
# NEVER use this configuration against a client's production network.
listener 1883
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
